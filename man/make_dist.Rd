% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{make_dist}
\alias{make_dist}
\title{Construct a distribution from partial specifications}
\usage{
make_dist(
  dist,
  mean = NULL,
  var = NULL,
  std = NULL,
  cv = NULL,
  scv = NULL,
  second_moment = NULL,
  median = NULL,
  q1 = NULL,
  q3 = NULL,
  iqr = NULL,
  quantiles = NULL,
  mode = NULL,
  support = NULL
)
}
\arguments{
\item{dist}{Character string naming the distribution (case-insensitive).
Supported: \code{"gamma"}, \code{"exponential"} (or \code{"exp"}),
\code{"logistic"}, \code{"beta"}.}

\item{mean}{Target mean.}

\item{var}{Target variance.}

\item{std}{Target standard deviation (converted to variance).}

\item{cv}{Coefficient of variation (requires \code{mean}).}

\item{scv}{Squared coefficient of variation (requires \code{mean}).}

\item{second_moment}{Target second raw moment (requires \code{mean}).}

\item{median}{Target median (p = 0.5 quantile).}

\item{q1}{First quartile (p = 0.25).}

\item{q3}{Third quartile (p = 0.75).}

\item{iqr}{Interquartile range.}

\item{quantiles}{List of two \code{c(p, q)} vectors for arbitrary quantile
constraints.}

\item{mode}{Target mode.}

\item{support}{Optional length-2 numeric vector \code{c(lo, hi)} giving the
target support; either endpoint may be \code{Inf}. When given, the
distribution is placed on this support via an affine transform (when the
requested shape matches the natural one) or truncation (when it is
strictly smaller). Currently used together with \code{mean} and
\code{var}.}
}
\value{
An object of class \code{"distsfactory_dist"} with elements:
\describe{
\item{\code{$d(x)}}{Density function}
\item{\code{$p(q)}}{CDF}
\item{\code{$q(p)}}{Quantile function}
\item{\code{$r(n)}}{Random generation}
\item{\code{$params}}{Named list of distribution parameters}
\item{\code{$name}}{Distribution name}
}
}
\description{
Creates a distribution object whose moments or quantiles match the
given constraints. Returns an object with \code{$d()}, \code{$p()},
\code{$q()}, \code{$r()} methods wrapping base R distribution functions,
plus a \code{$params} list.
}
\examples{
d <- make_dist("gamma", mean = 5, var = 3)
d$d(2)       # density at x = 2
d$p(5)       # CDF at x = 5
d$q(0.5)     # median
d$r(10)      # 10 random samples
d$params     # list(shape = ..., rate = ...)

}
